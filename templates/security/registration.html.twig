{% extends 'base.html.twig' %}

{% block body %}

    <h1>Inscription à l'application</h1>

    {{ form_start(registrationFrom) }}
    {{ form_row(registrationFrom.login, {'attr' : {
        'placeholder' : 'Votre nom d\'utilisateur'
    }}) }}
    {{ form_row(registrationFrom.password, {'attr' : {
        'placeholder' : 'Votre mots de passe'
    }}) }}
    {{ form_row(registrationFrom.confirmPassword, {'attr' : {
        'placeholder' : 'Confirmation du mots de passe'
    }}) }}
    <button type="submit" class="btn btn-success">Inscription</button>
    {{ form_end(registrationFrom) }}

{% endblock %}